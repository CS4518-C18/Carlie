<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.harryliu.carlie">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_geo_api_key" />

        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.passengerActivities.PlaceAutoCompleteActivity"
            android:parentActivityName=".activities.MainActivity" />
        <activity
            android:name=".activities.passengerActivities.CurrentTripActivity"
            android:parentActivityName=".activities.passengerActivities.RequestTripActivity" />
        <activity
            android:name=".activities.passengerActivities.RequestTripActivity"
            android:parentActivityName=".activities.MainActivity" />
        <activity
            android:name=".activities.passengerActivities.ConfirmRouteActivity"
            android:parentActivityName=".activities.passengerActivities.RequestTripActivity" />
        <activity
            android:name=".activities.driverActivities.NavigationActivity"
            android:parentActivityName=".activities.driverActivities.PassengerListActivity" />
        <activity android:name=".activities.driverActivities.PassengerListActivity" />
    </application>

</manifest>